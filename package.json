{
  "name": "react-pulldown-mobile",
  "version": "1.0.9",
  "description": "a react pulldown component",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && ./node_modules/.bin/babel src --copy-files --source-maps --extensions .es6,.es,.js,.jsx --out-dir dist && cp ./src/*.css ./dist/",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/59fe/react-pulldown.git"
  },
  "keywords": [
    "react",
    "pulldown"
  ],
  "author": "Margox",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/59fe/react-pulldown/issues"
  },
  "homepage": "https://github.com/59fe/react-pulldown#readme",
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0"
  }
}
